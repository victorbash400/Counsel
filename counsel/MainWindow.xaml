<Window x:Class="Counsel.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Counsel"
        mc:Ignorable="d"
        Title="Counsel" Height="850" Width="1470" MinWidth="1000" MinHeight="650" FontFamily="Segoe UI" Background="#2B2B2B"
        WindowStartupLocation="CenterScreen"
        WindowState="Normal"
        ResizeMode="CanResizeWithGrip">
    <Window.Resources>
        <!-- Existing resources unchanged -->
        <Color x:Key="Color.Primary">#A07F5F</Color>
        <Color x:Key="Color.Primary.Light">#C7B198</Color>
        <Color x:Key="Color.Accent">#E0B880</Color>
        <Color x:Key="Color.Sidebar.Background">#1E1E1E</Color>
        <Color x:Key="Color.App.Background">#2B2B2B</Color>
        <Color x:Key="Color.Surface">#383838</Color>
        <Color x:Key="Color.Surface.Light">#454545</Color>
        <Color x:Key="Color.Text.Primary">#EAEAEA</Color>
        <Color x:Key="Color.Text.Secondary">#A0A0A0</Color>
        <Color x:Key="Color.Text.Dark">#1E1E1E</Color>
        <Color x:Key="Color.Border">#4A4A4A</Color>
        <Color x:Key="Color.Highlight">#F0C88D</Color>
        <Color x:Key="Color.UserMessage.Background">#4A5F70</Color>
        <Color x:Key="Color.AgentMessage.Background">#3F3F3F</Color>
        <Color x:Key="Color.SystemMessage.Background">#4F4F4F</Color>
        <Color x:Key="Color.Success">#77A977</Color>
        <Color x:Key="Color.Error">#B96F6F</Color>
        <Color x:Key="AccentColor">#2196F3</Color>

        <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource Color.Primary}"/>
        <SolidColorBrush x:Key="PrimaryLightBrush" Color="{StaticResource Color.Primary.Light}"/>
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource Color.Accent}"/>
        <SolidColorBrush x:Key="SidebarBackgroundBrush" Color="{StaticResource Color.Sidebar.Background}"/>
        <SolidColorBrush x:Key="AppBackgroundBrush" Color="{StaticResource Color.App.Background}"/>
        <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource Color.Surface}"/>
        <SolidColorBrush x:Key="SurfaceLightBrush" Color="{StaticResource Color.Surface.Light}"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource Color.Text.Primary}"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource Color.Text.Secondary}"/>
        <SolidColorBrush x:Key="TextDarkBrush" Color="{StaticResource Color.Text.Dark}"/>
        <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource Color.Border}"/>
        <SolidColorBrush x:Key="HighlightBrush" Color="{StaticResource Color.Highlight}"/>
        <SolidColorBrush x:Key="UserMessageBgBrush" Color="{StaticResource Color.UserMessage.Background}"/>
        <SolidColorBrush x:Key="AgentMessageBgBrush" Color="{StaticResource Color.AgentMessage.Background}"/>
        <SolidColorBrush x:Key="SystemMessageBgBrush" Color="{StaticResource Color.SystemMessage.Background}"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource Color.Success}"/>
        <SolidColorBrush x:Key="ErrorBrush" Color="{StaticResource Color.Error}"/>
        <SolidColorBrush x:Key="DisabledForegroundBrush" Color="#707070"/>
        <SolidColorBrush x:Key="DisabledBackgroundBrush" Color="#404040"/>

        <DropShadowEffect x:Key="SubtleShadow" BlurRadius="8" ShadowDepth="2" Direction="270" Opacity="0.2" Color="#000000"/>
        <FontFamily x:Key="IconFont">Segoe MDL2 Assets</FontFamily>
        <CornerRadius x:Key="ControlCornerRadius">8</CornerRadius>
        <CornerRadius x:Key="CardCornerRadius">10</CornerRadius>
        <CornerRadius x:Key="BubbleCornerRadius">12</CornerRadius>

        <Style x:Key="BaseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextDarkBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="14,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="PART_Border" Background="{TemplateBinding Background}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                CornerRadius="{StaticResource ControlCornerRadius}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="PART_Border" Property="Background" Value="{StaticResource PrimaryLightBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="PART_Border" Property="Background" Value="{StaticResource DisabledBackgroundBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource DisabledForegroundBrush}"/>
                                <Setter Property="Opacity" Value="0.7"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="PART_Border" Property="Background" Value="{StaticResource PrimaryLightBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SoftIconButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontFamily" Value="{StaticResource IconFont}"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Width" Value="38"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="PART_Border" Background="{TemplateBinding Background}" CornerRadius="6">
                            <TextBlock Text="{TemplateBinding Content}"
                                       FontFamily="{TemplateBinding FontFamily}"
                                       FontSize="{TemplateBinding FontSize}"
                                       Foreground="{TemplateBinding Foreground}"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="PART_Border" Property="Background" Value="{StaticResource SurfaceLightBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="PART_Border" Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="{StaticResource DisabledForegroundBrush}"/>
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="PART_Border" Property="Background" Value="{StaticResource BorderBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SendButtonStyle" TargetType="Button" BasedOn="{StaticResource SoftIconButton}">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextDarkBrush}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryLightBrush}"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="{StaticResource DisabledBackgroundBrush}"/>
                    <Setter Property="Foreground" Value="{StaticResource DisabledForegroundBrush}"/>
                    <Setter Property="Opacity" Value="0.7"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryLightBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="AddButtonStyle" TargetType="Button" BasedOn="{StaticResource SoftIconButton}">
            <Setter Property="Content" Value=""/>
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style x:Key="ModeCardStyle" TargetType="RadioButton">
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="MinWidth" Value="90"/>
            <Setter Property="MinHeight" Value="90"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="PART_Border"
                                Background="{TemplateBinding Background}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                CornerRadius="{StaticResource ControlCornerRadius}"
                                Padding="{TemplateBinding Padding}">
                            <StackPanel HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}">
                                <TextBlock Text="{Binding Path=Tag, RelativeSource={RelativeSource TemplatedParent}}"
                                           FontFamily="{StaticResource IconFont}"
                                           HorizontalAlignment="Center"
                                           Margin="0,0,0,8" FontSize="24"/>
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                                  ContentSource="Content"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="PART_Border" Property="Background" Value="{StaticResource SurfaceLightBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                                <Setter TargetName="PART_Border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="PART_Border" Property="Background" Value="{StaticResource HighlightBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource TextDarkBrush}"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter TargetName="PART_Border" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                <Setter TargetName="PART_Border" Property="BorderThickness" Value="1.5"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="PART_Border" Property="Background" Value="{StaticResource DisabledBackgroundBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource DisabledForegroundBrush}"/>
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MessageBubbleBase" TargetType="Border">
            <Setter Property="Padding" Value="15,12"/>
            <Setter Property="Margin" Value="5,8"/>
            <Setter Property="MaxWidth" Value="700"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="CornerRadius" Value="{StaticResource BubbleCornerRadius}"/>
        </Style>
        <Style x:Key="UserMessageBubble" TargetType="Border" BasedOn="{StaticResource MessageBubbleBase}">
            <Setter Property="Background" Value="{StaticResource UserMessageBgBrush}"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="Margin" Value="100,5,15,10"/>
        </Style>
        <Style x:Key="AgentMessageBubble" TargetType="Border" BasedOn="{StaticResource MessageBubbleBase}">
            <Setter Property="Background" Value="{StaticResource AgentMessageBgBrush}"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Margin" Value="15,5,100,10"/>
        </Style>
        <Style x:Key="SystemMessageBubble" TargetType="Border" BasedOn="{StaticResource MessageBubbleBase}">
            <Setter Property="Background" Value="{StaticResource SystemMessageBgBrush}"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="100,8,100,12"/>
            <Setter Property="MaxWidth" Value="550"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Effect" Value="{x:Null}"/>
        </Style>

        <Style x:Key="BaseMessageText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="LineHeight" Value="22"/>
        </Style>
        <Style x:Key="UserMessageText" TargetType="TextBlock" BasedOn="{StaticResource BaseMessageText}"/>
        <Style x:Key="AgentMessageText" TargetType="TextBlock" BasedOn="{StaticResource BaseMessageText}"/>
        <Style x:Key="SystemMessageText" TargetType="TextBlock" BasedOn="{StaticResource BaseMessageText}">
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="FontSize" Value="12.5"/>
            <Setter Property="TextAlignment" Value="Center"/>
        </Style>
        <Style x:Key="AgentNameText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
            <Setter Property="FontSize" Value="12.5"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>
        <Style x:Key="ThinkingIndicatorText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontStyle" Value="Italic"/>
            <Setter Property="Margin" Value="20,8,5,12"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>
        <Style x:Key="SectionHeaderLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <Style x:Key="SoftListItem" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="13.5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource HighlightBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource TextDarkBrush}"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource SurfaceLightBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CopyButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontFamily" Value="{StaticResource IconFont}"/>
            <Setter Property="Content" Value=""/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="Margin" Value="0,-8,-8,0"/>
            <Setter Property="Visibility" Value="Hidden"/>
            <Setter Property="ToolTip" Value="Copy text"/>
            <Setter Property="Width" Value="28"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="PART_Border" Background="{TemplateBinding Background}" CornerRadius="4">
                            <TextBlock Text="{TemplateBinding Content}"
                                       FontFamily="{TemplateBinding FontFamily}"
                                       FontSize="{TemplateBinding FontSize}"
                                       Foreground="{TemplateBinding Foreground}"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="PART_Border" Property="Background" Value="{StaticResource SurfaceLightBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="PART_Border" Property="Background" Value="{StaticResource HighlightBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource TextDarkBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="CaretBrush" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="AllowDrop" Value="True"/>
            <Setter Property="ScrollViewer.PanningMode" Value="VerticalFirst"/>
            <Setter Property="Stylus.IsFlicksEnabled" Value="False"/>
            <Setter Property="SelectionBrush" Value="{StaticResource HighlightBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{StaticResource ControlCornerRadius}"
                                SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Focusable="False"
                                          HorizontalScrollBarVisibility="Hidden"
                                          VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                <Setter TargetName="Bd" Property="BorderThickness" Value="1.5"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource DisabledBackgroundBrush}"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource DisabledForegroundBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ComboBoxToggleButton" TargetType="ToggleButton">
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="IsTabStop" Value="false"/>
            <Setter Property="Focusable" Value="false"/>
            <Setter Property="ClickMode" Value="Press"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="templateRoot"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{StaticResource ControlCornerRadius}"
                                SnapsToDevicePixels="true">
                            <Border x:Name="splitBorder"
                                    BorderBrush="Transparent"
                                    BorderThickness="1"
                                    HorizontalAlignment="Right"
                                    Margin="0"
                                    SnapsToDevicePixels="true"
                                    Width="{DynamicResource {x:Static SystemParameters.VerticalScrollBarWidthKey}}">
                                <Path x:Name="arrow"
                                      Data="F1 M 0,0 L 2.667,2.667 L 5.333,0 L 0,0 Z"
                                      Fill="{StaticResource TextSecondaryBrush}"
                                      HorizontalAlignment="Center"
                                      Margin="0"
                                      VerticalAlignment="Center"/>
                            </Border>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="arrow" Property="Fill" Value="{StaticResource TextPrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="true">
                                <Setter TargetName="arrow" Property="Fill" Value="{StaticResource TextPrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter TargetName="arrow" Property="Fill" Value="{StaticResource DisabledForegroundBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
            <Setter Property="ScrollViewer.PanningMode" Value="Both"/>
            <Setter Property="Stylus.IsFlicksEnabled" Value="False"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource SoftListItem}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid x:Name="MainGrid" SnapsToDevicePixels="true">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition MinWidth="{DynamicResource {x:Static SystemParameters.VerticalScrollBarWidthKey}}" Width="0"/>
                            </Grid.ColumnDefinitions>
                            <Popup x:Name="PART_Popup" AllowsTransparency="true" Grid.ColumnSpan="2" IsOpen="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" Margin="1" PopupAnimation="{DynamicResource {x:Static SystemParameters.ComboBoxPopupAnimationKey}}" Placement="Bottom">
                                <Border x:Name="DropDownBorder" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <ScrollViewer x:Name="DropDownScrollViewer">
                                        <ItemsPresenter x:Name="ItemsPresenter" KeyboardNavigation.DirectionalNavigation="Contained" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                            <ToggleButton x:Name="toggleButton" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Grid.ColumnSpan="2" IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" Style="{StaticResource ComboBoxToggleButton}"/>
                            <ContentPresenter x:Name="contentPresenter" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" Content="{TemplateBinding SelectionBoxItem}" ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" IsHitTestVisible="false" Margin="{TemplateBinding Padding}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="HasItems" Value="false">
                                <Setter Property="Height" TargetName="DropDownBorder" Value="95"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Opacity" Value="0.6"/>
                                <Setter Property="Foreground" Value="{StaticResource DisabledForegroundBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" TargetName="toggleButton" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter Property="BorderBrush" TargetName="toggleButton" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ListBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource SoftListItem}"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBox">
                        <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" Padding="1" SnapsToDevicePixels="true">
                            <ScrollViewer Focusable="false" Padding="{TemplateBinding Padding}">
                                <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern Scrollbar Style -->
        <Style x:Key="ModernScrollBarStyle" TargetType="{x:Type ScrollBar}">
            <Setter Property="Stylus.IsPressAndHoldEnabled" Value="false"/>
            <Setter Property="Stylus.IsFlicksEnabled" Value="false"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="8"/>
            <Setter Property="MinWidth" Value="8"/>
            <Setter Property="Opacity" Value="0.4"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="1"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Button style (for scroll buttons) -->
        <Style x:Key="ModernScrollBarRepeatButtonStyle" TargetType="{x:Type RepeatButton}">
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Focusable" Value="false"/>
            <Setter Property="IsTabStop" Value="false"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RepeatButton}">
                        <Rectangle Fill="{TemplateBinding Background}" Height="{TemplateBinding Height}" Width="{TemplateBinding Width}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Thumb style (the draggable part) -->
        <Style x:Key="ModernScrollBarThumbStyle" TargetType="{x:Type Thumb}">
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="IsTabStop" Value="false"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Border x:Name="rectangle" 
                            Background="{StaticResource BorderBrush}" 
                            CornerRadius="4" 
                            Height="{TemplateBinding Height}" 
                            SnapsToDevicePixels="True" 
                            Width="{TemplateBinding Width}">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{StaticResource TextSecondaryBrush}"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="0" BlurRadius="3" Opacity="0.2" Color="Black"/>
                            </Border.Effect>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ScrollViewer Template -->
        <Style TargetType="ScrollViewer">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollViewer">
                        <Grid x:Name="Grid" Background="{TemplateBinding Background}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <ScrollContentPresenter x:Name="PART_ScrollContentPresenter" 
                                               CanContentScroll="{TemplateBinding CanContentScroll}" 
                                               CanHorizontallyScroll="False" 
                                               CanVerticallyScroll="False" 
                                               ContentTemplate="{TemplateBinding ContentTemplate}" 
                                               Content="{TemplateBinding Content}" 
                                               Grid.Column="0" 
                                               Margin="{TemplateBinding Padding}" 
                                               Grid.Row="0"/>

                            <ScrollBar x:Name="PART_VerticalScrollBar" 
                                  AutomationProperties.AutomationId="VerticalScrollBar" 
                                  Cursor="Arrow" 
                                  Grid.Column="1" 
                                  Maximum="{TemplateBinding ScrollableHeight}" 
                                  Minimum="0" 
                                  Grid.Row="0" 
                                  Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}" 
                                  Value="{Binding VerticalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}" 
                                  ViewportSize="{TemplateBinding ViewportHeight}" 
                                  Style="{StaticResource ModernScrollBarStyle}"/>

                            <ScrollBar x:Name="PART_HorizontalScrollBar" 
                                  AutomationProperties.AutomationId="HorizontalScrollBar" 
                                  Cursor="Arrow" 
                                  Grid.Column="0" 
                                  Maximum="{TemplateBinding ScrollableWidth}" 
                                  Minimum="0" 
                                  Orientation="Horizontal" 
                                  Grid.Row="1" 
                                  Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}" 
                                  Value="{Binding HorizontalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}" 
                                  ViewportSize="{TemplateBinding ViewportWidth}" 
                                  Style="{StaticResource ModernScrollBarStyle}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid x:Name="MainGrid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280" MinWidth="260" x:Name="SidebarColumn"/>
            <ColumnDefinition Width="*" x:Name="SearchColumn"/>
            <ColumnDefinition Width="400" x:Name="CanvasColumn"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Background="{StaticResource SidebarBackgroundBrush}">
            <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,1,0">
                <Grid Margin="15,15,15,10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="5,0,0,20">
                        <TextBlock Text="Counsel" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"
                                   VerticalAlignment="Center" HorizontalAlignment="Left" Margin="0,0,0,0"/>
                    </Grid>

                    <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}" CornerRadius="{StaticResource CardCornerRadius}" Padding="15" Margin="0,0,0,12" Effect="{StaticResource SubtleShadow}">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <TextBlock Text="" FontFamily="{StaticResource IconFont}" FontSize="14" Foreground="{StaticResource AccentBrush}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Case Management" Style="{StaticResource SectionHeaderLabel}" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                            </StackPanel>
                            <ComboBox x:Name="CaseSelectorComboBox" SelectedIndex="0"
                                      SelectionChanged="CaseSelectorComboBox_SelectionChanged">
                                <ComboBoxItem Content="Case C123 - Johnson v. Acme"/>
                                <ComboBoxItem Content="Case D456 - Miller Estate"/>
                                <ComboBoxItem Content="Case E789 - State v. Davis"/>
                                <Separator/>
                                <ComboBoxItem Content="[ Open Existing Case... ]" Foreground="{StaticResource PrimaryBrush}"/>
                                <ComboBoxItem Content="[ Start New Case... ]" Foreground="{StaticResource PrimaryBrush}"/>
                            </ComboBox>
                        </StackPanel>
                    </Border>

                    <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}" CornerRadius="{StaticResource CardCornerRadius}" Padding="15" Margin="0,0,0,12" Effect="{StaticResource SubtleShadow}">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <TextBlock Text="" FontFamily="{StaticResource IconFont}" FontSize="14" Foreground="{StaticResource AccentBrush}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Modes" Style="{StaticResource SectionHeaderLabel}" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                            </StackPanel>
                            <UniformGrid Columns="2" HorizontalAlignment="Center">
                                <RadioButton x:Name="ChatOnlyModeButton" Content="Chat Only" GroupName="ModeGroup"
                                             Style="{StaticResource ModeCardStyle}" Tag="" Checked="ModeButton_Checked" ToolTip="Standard chat interface without the canvas"/>
                                <RadioButton x:Name="DeepResearchModeButton" Content="Research" GroupName="ModeGroup"
                                             Style="{StaticResource ModeCardStyle}" Tag="" Checked="ModeButton_Checked" ToolTip="Perform in-depth legal research"/>
                                <RadioButton x:Name="ParalegalModeButton" Content="Paralegal" GroupName="ModeGroup"
                                             Style="{StaticResource ModeCardStyle}" Tag="" Checked="ModeButton_Checked" ToolTip="Assist with document summary and evidence"/>
                                <RadioButton x:Name="CrossExamineModeButton" Content="Examine" GroupName="ModeGroup"
                                             Style="{StaticResource ModeCardStyle}" Tag="" Checked="ModeButton_Checked" ToolTip="Analyze testimony and find inconsistencies"/>
                            </UniformGrid>
                        </StackPanel>
                    </Border>

                    <Border Grid.Row="3" Background="{StaticResource SurfaceBrush}" CornerRadius="{StaticResource CardCornerRadius}" Padding="15" Margin="0,0,0,12" Effect="{StaticResource SubtleShadow}">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <TextBlock Text="" FontFamily="{StaticResource IconFont}" FontSize="14" Foreground="{StaticResource AccentBrush}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Case Documents" Style="{StaticResource SectionHeaderLabel}" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                            </StackPanel>
                           
                            <Button Content=" Upload Document" Style="{StaticResource BaseButtonStyle}" Margin="0,10,0,0"
                                    Click="UploadDocument_Click" FontWeight="Normal" FontFamily="{StaticResource IconFont}"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Row="4" Background="{StaticResource SurfaceBrush}" CornerRadius="{StaticResource CardCornerRadius}" Padding="15" Margin="0,0,0,12" Effect="{StaticResource SubtleShadow}">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <TextBlock Text="&#xE163;" FontFamily="{StaticResource IconFont}" FontSize="14" Foreground="{StaticResource AccentBrush}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Tasks &amp; Calendar" Style="{StaticResource SectionHeaderLabel}" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                            </StackPanel>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <ComboBox x:Name="TaskComboBox" Grid.Column="0" Margin="0,0,10,0">
                                    <ComboBoxItem Content="[High Priority] File response brief by EOD"/>
                                    <ComboBoxItem Content="[Completed] Summarize expert report"/>
                                    <ComboBoxItem Content="[Scheduled] Client meeting tomorrow 10 AM"/>
                                </ComboBox>
                                <Button x:Name="AddTaskButton" Grid.Column="1" Style="{StaticResource AddButtonStyle}" ToolTip="Add Event" Click="AddTaskButton_Click"/>
                            </Grid>
                            <Popup x:Name="TaskPopup" StaysOpen="False" PlacementTarget="{Binding ElementName=AddTaskButton}" Placement="Top" AllowsTransparency="True">
                                <Border Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="15">
                                    <StackPanel Width="300">
                                        <TextBlock Text="Add Calendar Event" Foreground="{StaticResource TextPrimaryBrush}" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,10"/>
                                        <TextBlock Text="Title" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,5"/>
                                        <TextBox x:Name="EventTitleTextBox" Margin="0,0,0,10" TextWrapping="Wrap"/>
                                        <TextBlock Text="Date (e.g., 2025-04-18 or tomorrow)" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,5"/>
                                        <TextBox x:Name="EventDateTextBox" Margin="0,0,0,10" TextWrapping="Wrap"/>
                                        <TextBlock Text="Time (e.g., 10:00 AM)" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,5"/>
                                        <TextBox x:Name="EventTimeTextBox" Margin="0,0,0,10" TextWrapping="Wrap"/>
                                        <TextBlock Text="Description (or paste natural language query)" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,5"/>
                                        <TextBox x:Name="EventDescriptionTextBox" MinHeight="60" AcceptsReturn="True" TextWrapping="Wrap" Margin="0,0,0,10"/>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                            <Button Content="Cancel" Style="{StaticResource BaseButtonStyle}" Margin="0,0,10,0" Click="CancelTaskButton_Click"/>
                                            <Button Content="Add" Style="{StaticResource BaseButtonStyle}" Click="ConfirmTaskButton_Click"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </Popup>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>
        </Grid>

        <Grid Grid.Column="1" Background="{StaticResource AppBackgroundBrush}">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="Transparent" Padding="0" Margin="10,10,10,5">
                <ScrollViewer x:Name="ConversationScrollViewer" VerticalScrollBarVisibility="Auto" Padding="10,5">
                    <StackPanel x:Name="ConversationPanel">
                        <Border Style="{StaticResource AgentMessageBubble}" x:Name="InitialWelcomeMessage"
                                MouseEnter="AgentBubble_MouseEnter" MouseLeave="AgentBubble_MouseLeave">
                            <StackPanel>
                                <TextBlock Style="{StaticResource AgentNameText}">Counsel</TextBlock>
                                <TextBlock x:Name="WelcomeMessageText" Style="{StaticResource AgentMessageText}"
                                           Text="Welcome! Select a case and mode from the left sidebar to begin. &#x0a;You can ask me to: &#x0a; • Summarize documents &#x0a; • Draft legal briefs &#x0a; • Analyze testimony"/>
                                <Button x:Name="CopyWelcomeTextButton" Style="{StaticResource CopyButtonStyle}"
                                        Click="CopyAgentTextButton_Click" Tag="{Binding ElementName=WelcomeMessageText}"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}"
                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" Padding="15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Background="{StaticResource AppBackgroundBrush}" CornerRadius="{StaticResource ControlCornerRadius}"
                            Padding="0" Margin="0,0,10,0" BorderThickness="1" BorderBrush="{StaticResource BorderBrush}">
                        <Grid>
                            <TextBlock x:Name="InputPlaceholder" Text="Ask Counsel..." Foreground="{StaticResource TextSecondaryBrush}"
                                       FontSize="14" VerticalAlignment="Center" Margin="12,0,0,0" Opacity="0.8" IsHitTestVisible="False"/>
                            <TextBox x:Name="RealInputTextBox"
                                     Background="Transparent"
                                     BorderThickness="0"
                                     VerticalContentAlignment="Center"
                                     FontSize="14"
                                     Padding="12,10"
                                     Foreground="{StaticResource TextPrimaryBrush}" AcceptsReturn="True"
                                     TextWrapping="Wrap"
                                     SpellCheck.IsEnabled="True"
                                     GotFocus="RealInputTextBox_GotFocus"
                                     LostFocus="RealInputTextBox_LostFocus"
                                     TextChanged="RealInputTextBox_TextChanged"
                                     KeyDown="RealInputTextBox_KeyDown"
                                     MinHeight="42" MaxHeight="120"
                                     Style="{x:Null}"/>
                        </Grid>
                    </Border>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Bottom" Margin="0,0,0,1">
                        <Button x:Name="AttachButton" Content="" Style="{StaticResource SoftIconButton}" ToolTip="Attach File" Click="AttachButton_Click"/>
                        <Button x:Name="SendButton" Content="" Style="{StaticResource SendButtonStyle}" ToolTip="Send Message" IsEnabled="False" Click="SendButton_Click" Margin="5,0,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Column="2" Background="{StaticResource SidebarBackgroundBrush}"
        BorderBrush="{StaticResource BorderBrush}" BorderThickness="1,0,0,0"
        x:Name="RightSidebar" Visibility="Visible">
            <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="0,0,2,0">
                <Grid Margin="15">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Active Context Section with improved styling -->
                    <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}" 
                    CornerRadius="{StaticResource CardCornerRadius}" 
                    Padding="15" Margin="0,15,0,12" 
                    Effect="{StaticResource SubtleShadow}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Header with gradient accent line -->
                            <Grid Grid.Row="0" Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Orientation="Horizontal" Grid.Column="0">
                                    <Border Width="3" Height="16" Margin="0,0,10,0" CornerRadius="2">
                                        <Border.Background>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                <GradientStop Color="{StaticResource AccentColor}" Offset="0"/>
                                                <GradientStop Color="#4CAF50" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Border.Background>
                                    </Border>
                                    <TextBlock Text="Active Context" Style="{StaticResource SectionHeaderLabel}" 
                                       Foreground="{StaticResource TextPrimaryBrush}" 
                                       FontWeight="SemiBold" VerticalAlignment="Center"/>
                                </StackPanel>

                                <TextBlock Grid.Column="1" Text="" 
                                   FontFamily="{StaticResource IconFont}" FontSize="14" 
                                   Foreground="{StaticResource AccentBrush}"/>
                            </Grid>

                            <!-- Context Info with better spacing and visuals -->
                            <StackPanel Grid.Row="1">
                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="" FontFamily="{StaticResource IconFont}" 
                                       FontSize="12" Foreground="{StaticResource AccentBrush}" 
                                       Margin="0,0,8,0" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="1" x:Name="ContextCaseText" 
                                       Text="Case: [Select a Case]" FontSize="13"
                                       Foreground="{StaticResource TextPrimaryBrush}" 
                                       FontWeight="SemiBold"/>
                                </Grid>

                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="" FontFamily="{StaticResource IconFont}" 
                                       FontSize="12" Foreground="{StaticResource AccentBrush}" 
                                       Margin="0,0,8,0" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="1" x:Name="ContextModeText" 
                                       Text="Mode: [Select a Mode]" FontSize="13"
                                       Foreground="{StaticResource TextPrimaryBrush}"/>
                                </Grid>

                                <Grid Margin="0,0,0,0" Visibility="Collapsed">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="" FontFamily="{StaticResource IconFont}" 
                                       FontSize="12" Foreground="{StaticResource AccentBrush}" 
                                       Margin="0,0,8,0" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="1" x:Name="ContextFocusText" 
                                       Text="Focus: [Select a Document]" FontSize="13"
                                       Foreground="{StaticResource TextPrimaryBrush}"/>
                                </Grid>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Canvas Section Header with improved styling -->
                    <Grid Grid.Row="1" Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Orientation="Horizontal" Grid.Column="0">
                            <Border Width="3" Height="16" Margin="0,0,10,0" CornerRadius="2">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                        <GradientStop Color="{StaticResource AccentColor}" Offset="0"/>
                                        <GradientStop Color="#2196F3" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                            </Border>
                            <TextBlock x:Name="RightSidebarTitle" Text="Canvas" 
                               Style="{StaticResource SectionHeaderLabel}" 
                               Foreground="{StaticResource TextPrimaryBrush}" 
                               FontWeight="SemiBold" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Grid>

                    <!-- Canvas Content with copy button -->
                    <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}" 
                    CornerRadius="{StaticResource CardCornerRadius}" 
                    Padding="0" VerticalAlignment="Stretch" 
                    Effect="{StaticResource SubtleShadow}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- Copy Button Header Bar -->
                            <Border Grid.Row="0" Background="{StaticResource SurfaceLightBrush}" 
                            BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1"
                            CornerRadius="8,8,0,0"
                            Padding="15,8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Text="Output" FontSize="12" 
                                       Foreground="{StaticResource TextSecondaryBrush}" 
                                       FontWeight="SemiBold" VerticalAlignment="Center"/>

                                    <Button Grid.Column="1" Padding="8,4" 
                                    Background="Transparent" BorderThickness="0"
                                    ToolTip="Copy content to clipboard"
                                    Click="CopyButton_Click">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="" FontFamily="{StaticResource IconFont}" 
                                               FontSize="12" Foreground="{StaticResource AccentBrush}" 
                                               Margin="0,0,5,0" VerticalAlignment="Center"/>
                                            <TextBlock Text="Copy" FontSize="11" 
                                               Foreground="{StaticResource TextSecondaryBrush}"/>
                                        </StackPanel>
                                    </Button>
                                </Grid>
                            </Border>

                            <!-- Canvas Content -->
                            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="15">
                                <TextBlock x:Name="RightSidebarContent" TextWrapping="Wrap"
                                   Foreground="{StaticResource TextPrimaryBrush}" 
                                   FontSize="13.5" LineHeight="22"
                                   Text="[Canvas output based on mode will appear here.]"/>
                            </ScrollViewer>
                        </Grid>
                    </Border>
                </Grid>
            </ScrollViewer>
        </Border>

    </Grid>
</Window>